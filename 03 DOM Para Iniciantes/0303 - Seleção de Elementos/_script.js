const animais = document.getElementById('animais')
console.log(animais)


// Vai puxar somente elementos que possuírem as duas classes. Se possui somnete uma delas não vai puxar.
const contato = document.getElementsByClassName('grid-section contato')

// Query selector pega somente o primeiro item que aparece no css do pedido.
const linkCSS = document.querySelector('[href^="#"]')


// Pega todas as UL's da pagina em uma NodeList
const listas = document.querySelectorAll('ul')

 